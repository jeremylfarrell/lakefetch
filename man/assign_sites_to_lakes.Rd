% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lake_sources.R
\name{assign_sites_to_lakes}
\alias{assign_sites_to_lakes}
\title{Assign Sites to Lakes}
\usage{
assign_sites_to_lakes(sites_sf, water_polygons, tolerance_m = NULL)
}
\arguments{
\item{sites_sf}{sf object with site points}

\item{water_polygons}{sf object with lake polygons}

\item{tolerance_m}{Buffer distance for matching sites near lake edges}
}
\value{
sf object with sites and added columns for lake_osm_id, lake_name, lake_area_km2
}
\description{
Perform spatial join to assign each site to its containing lake polygon.
}
\examples{
if (FALSE) {
# After downloading lake boundaries
lake_data <- get_lake_boundary(sites)

# Assign sites to their containing lakes
sites_assigned <- assign_sites_to_lakes(
  lake_data$sites,
  lake_data$all_lakes,
  tolerance_m = 50
)

# Check assignments
table(sites_assigned$lake_name)
}

}
