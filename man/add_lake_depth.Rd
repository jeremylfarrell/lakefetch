% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bathymetry.R
\name{add_lake_depth}
\alias{add_lake_depth}
\title{Add Depth Information to Fetch Results}
\usage{
add_lake_depth(fetch_results, lakes, user_depths = NULL)
}
\arguments{
\item{fetch_results}{sf object with fetch results}

\item{lakes}{sf object with lake polygons}

\item{user_depths}{Named vector of user-provided depths (names = lake IDs)}
}
\value{
fetch_results with added depth columns
}
\description{
Looks up or estimates depth for each lake in the fetch results and
adds depth columns.
}
\examples{
\donttest{
data(adirondack_sites)
sites <- load_sites(adirondack_sites)
lake <- get_lake_boundary(sites)
results <- fetch_calculate(sites, lake)

# Add depth estimates
results$results <- add_lake_depth(results$results, results$lakes)

# Or provide known depths using an actual lake_osm_id from results
lake_id <- results$lakes$osm_id[1]
depths <- setNames(15.5, lake_id)
results$results <- add_lake_depth(results$results, results$lakes, user_depths = depths)
}

}
